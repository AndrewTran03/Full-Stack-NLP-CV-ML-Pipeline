# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build server-node-nx-standalone`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t server-node-nx-standalone`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system server-node-nx-standalone && \
    adduser --system -G server-node-nx-standalone server-node-nx-standalone

COPY dist/server-node-nx-standalone server-node-nx-standalone/
RUN chown -R server-node-nx-standalone:server-node-nx-standalone .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix server-node-nx-standalone --omit=dev -f install

CMD [ "node", "server-node-nx-standalone" ]
